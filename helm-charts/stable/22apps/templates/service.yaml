apiVersion: v1
kind: Service
metadata:
  name: {{ include "22apps.fullname" . }}
  labels:
    {{- include "22apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "22apps.selectorLabels" . | nindent 4 }}
